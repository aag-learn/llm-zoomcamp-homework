# `SECRET_*` values are for Kestra's `secret()` function in Open Source.
# Encode the raw API key with no trailing newline and no wrapped output,
# e.g. `echo -n "$OPENAI_API_KEY" | base64 -w0`.
GEMINI_API_KEY=""
SECRET_GEMINI_API_KEY=""
SECRET_TAVILY_API_KEY=""
SECRET_OPENAI_API_KEY=""
